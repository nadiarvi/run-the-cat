// const colors = {
//     primary: "#372A30",
//     secondary: "#AA644D",
//     tertiary: "#788374",
//     accent: "#F5E9BF"
// };

const colors = {
    Pico8Style: {
    //     primary: "#4e8cff",    // Light blue (sky / background)
    // secondary: "#ff4f64",  // Bright red (player, key objects)
    // tertiary: "#f8d34e",   // Bright yellow (coins, details)
    // accent: "#3c9b76"      // Soft green (platforms, foliage)
    primary: "#a6d1ff",    // Very light pastel blue (sky / background)
    // secondary: "#ffb3b3",  // Soft pastel red (player / key objects)
    secondary: "#ff4f64",  // Bright red (player, key objects)
    tertiary: "#fff29d",   // Soft pastel yellow (coins / highlights)
    accent: "#a4e4b6"      // Light pastel green (platforms / foliage)
    },
    GameBoyStyle: {
      primary: "#0f380f",    // Very dark green (background)
      secondary: "#306230",  // Medium green (ground/platform)
      tertiary: "#8bac0f",   // Light green (sky or decor)
      accent: "#9bbc0f"      // Yellow-green (player / highlight)
    },
    PastelPixelStyle: {
      primary: "#4e495f",    // Muted purple-gray (background)
      secondary: "#7a5c58",  // Warm brown (platform)
      tertiary: "#82a3a1",   // Dusty teal (sky/detail)
      accent: "#f6c28b"      // Light peach (player)
    }
};

export {
    colors
}